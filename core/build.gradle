plugins {
    id 'java-library'
}

dependencies {
    api project(':base')
    api project(':user-info')

    api libs.bundles.pekkoBundle
    api libs.apacheCommons
    api libs.uuidCreator

    api libs.postgres
    api libs.hikari
    api libs.liquibase
    api libs.kafkaClients

    implementation libs.protobuf
}
